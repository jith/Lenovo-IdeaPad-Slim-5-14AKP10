# Load the script that carries the listening level across an output change.
# See /usr/local/share/wireplumber/scripts/54-volume-sync.lua.

wireplumber.components = [
  {
    name = 54-volume-sync.lua, type = script/lua
    provides = custom.volume-sync
  }
]

wireplumber.profiles = {
  main = {
    custom.volume-sync = required
  }
}
